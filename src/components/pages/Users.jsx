import styled from "styled-componets";

export const Users = () => {
  return (
    <SContainer>
      <h2>Topページ</h2>
    </SContainer>
  );
};

const SContainer = styled.div`
  text-align: center;
`;
